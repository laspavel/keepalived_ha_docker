version: '3'
services:
  haproxy:
    image: "haproxy:2.2-alpine"
    network_mode: "host"
    restart: always
    volumes:
     - ./data:/var/lib/haproxy
     - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg